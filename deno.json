{
	"tasks": {
		"run": "deno run --allow-read --allow-write --allow-run --allow-env --config deno.json",
		"format": "deno fmt --config deno.json"
	},
	"fmt": {
		"options": {
			"useTabs": true,
			"lineWidth": 100,
			"indentWidth": 2
		}
	}
}
